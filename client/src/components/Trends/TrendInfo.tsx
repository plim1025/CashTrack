// REACT //
import React from 'react';

// TYPES //
import { Transaction } from '../../types';

interface Props {
    transactions: Transaction[];
}

const TrendInfo: React.FC<Props> = props => {
    return <></>;
};

export default TrendInfo;
